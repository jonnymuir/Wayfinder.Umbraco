@model Wayfinder.Umbraco.Models.ComponentContext
@{
    var stats = Model.Component.Stats ?? Array.Empty<Wayfinder.Models.ServiceDesign.StatItem>();
}

@if (!string.IsNullOrEmpty(Model.Component.Title))
{
    <h2 class="govuk-heading-m">@Model.Component.Title</h2>
}

<div class="wayfinder-stat-group" data-wayfinder-stat-group role="group" aria-label="@(Model.Component.Title ?? "Key figures")" aria-live="polite">
    @foreach (var stat in stats)
    {
        <div class="wayfinder-stat-card@(stat.Emphasis ? " wayfinder-stat-card--emphasis" : "")" data-wayfinder-stat="@stat.Label" data-wayfinder-stat-field="@stat.FieldKey">
            <div class="wayfinder-stat-card__label">@stat.Label</div>
            <div class="wayfinder-stat-card__value">@(string.IsNullOrEmpty(stat.Value) ? "—" : stat.Value)</div>
            @if (!string.IsNullOrEmpty(stat.Qualifier))
            {
                <div class="wayfinder-stat-card__qualifier">@stat.Qualifier</div>
            }
        </div>
    }
</div>
